@model UpdateMemberDto
@{
    var error = TempData["Error"] as string;
}

<div class="receipt-card">
    <div class="receipt-header">
        <h4><i class="bi bi-pencil-square"></i> @SharedLocalizer["Edit Member"]</h4>
        <span class="receipt-badge">@SharedLocalizer["Receipt"]</span>
    </div>

    <form asp-action="Edit" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data" class="receipt-body">
        <input type="hidden" asp-for="Id" />

                @if (!string.IsNullOrEmpty(error))
                {
                    <div class="alert alert-danger">@error</div>
                }

        <div class="section-title"><i class="bi bi-person-badge"></i> @SharedLocalizer["Basic Information"]</div>
        <div class="row g-3">
            <div class="col-md-4">
                <label class="form-label">@SharedLocalizer["Member Image"]</label>
                <div class="image-dropzone" id="imageDropzone">
                    <input type="file" name="imageFile" id="imageInput" accept="image/*" hidden />
                    <div class="dropzone-placeholder" id="dropzonePlaceholder" @(!string.IsNullOrEmpty(Model.ImagePath) ? @"style=""display:none""" : "")>
                        <i class="bi bi-cloud-upload fs-1 text-muted"></i>
                        <p class="mb-0 mt-2">@SharedLocalizer["Drag & drop or click to upload"]</p>
                        <small class="text-muted">@SharedLocalizer["JPG, PNG or GIF"]</small>
                    </div>
                    <div class="dropzone-preview" id="dropzonePreview" @(string.IsNullOrEmpty(Model.ImagePath) ? @"style=""display:none""" : "")>
                        <img id="imagePreview" src="@(!string.IsNullOrEmpty(Model.ImagePath) ? Url.Content("~/" + Model.ImagePath) : "")" alt="Preview" />
                        <button type="button" class="btn btn-sm btn-outline-danger mt-2" id="removeImage">@SharedLocalizer["Remove"]</button>
                    </div>
                </div>
            </div>
            <div class="col-md-8">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label asp-for="FullName" class="form-label"></label>
                        <input asp-for="FullName" class="form-control" />
                        <span asp-validation-for="FullName" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="PhoneNumber" class="form-label">@SharedLocalizer["Phone"]</label>
                        <input asp-for="PhoneNumber" class="form-control" maxlength="11" />
                        <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Email" class="form-label"></label>
                        <input asp-for="Email" class="form-control" type="email" />
                    </div>
                    <div class="col-md-3">
                        <label asp-for="DateOfBirth" class="form-label">@SharedLocalizer["Date of Birth"]</label>
                        <input asp-for="DateOfBirth" class="form-control" type="date" />
                    </div>
                    <div class="col-md-3">
                        <label asp-for="Gender" class="form-label"></label>
                        <select asp-for="Gender" class="form-select">
                            <option value="">@SharedLocalizer["Select gender"]</option>
                            <option value="Male">@SharedLocalizer["Male"]</option>
                            <option value="Female">@SharedLocalizer["Female"]</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Nationality" class="form-label">@SharedLocalizer["Nationality"] <small class="text-muted">(@SharedLocalizer["optional"])</small></label>
                        <select asp-for="Nationality" class="form-select">
                            <option value="">@SharedLocalizer["Select nationality"]</option>
                            <option value="Egyptian">@SharedLocalizer["Egyptian"]</option>
                            <option value="Saudi">@SharedLocalizer["Saudi"]</option>
                            <option value="Emirati">@SharedLocalizer["Emirati"]</option>
                            <option value="Kuwaiti">@SharedLocalizer["Kuwaiti"]</option>
                            <option value="Qatari">@SharedLocalizer["Qatari"]</option>
                            <option value="Bahraini">@SharedLocalizer["Bahraini"]</option>
                            <option value="Omani">@SharedLocalizer["Omani"]</option>
                            <option value="Jordanian">@SharedLocalizer["Jordanian"]</option>
                            <option value="Lebanese">@SharedLocalizer["Lebanese"]</option>
                            <option value="Syrian">@SharedLocalizer["Syrian"]</option>
                            <option value="Palestinian">@SharedLocalizer["Palestinian"]</option>
                            <option value="Iraqi">@SharedLocalizer["Iraqi"]</option>
                            <option value="Yemeni">@SharedLocalizer["Yemeni"]</option>
                            <option value="Libyan">@SharedLocalizer["Libyan"]</option>
                            <option value="Tunisian">@SharedLocalizer["Tunisian"]</option>
                            <option value="Algerian">@SharedLocalizer["Algerian"]</option>
                            <option value="Moroccan">@SharedLocalizer["Moroccan"]</option>
                            <option value="Sudanese">@SharedLocalizer["Sudanese"]</option>
                            <option value="Other">@SharedLocalizer["Other"]</option>
                        </select>
                        <span asp-validation-for="Nationality" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="NationalId" class="form-label">@SharedLocalizer["National ID"] <small class="text-muted">(@SharedLocalizer["optional"])</small></label>
                        <input asp-for="NationalId" class="form-control" maxlength="14" />
                        <span asp-validation-for="NationalId" class="text-danger small"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="ReferralSource" class="form-label">@SharedLocalizer["Referral Source"]</label>
                        <select asp-for="ReferralSource" class="form-select">
                            <option value="">@SharedLocalizer["Select referral source"]</option>
                            <option value="Social Media">@SharedLocalizer["Social Media"]</option>
                            <option value="Friend">@SharedLocalizer["Friend"]</option>
                            <option value="Walk-in">@SharedLocalizer["Walk-in"]</option>
                            <option value="Advertisement">@SharedLocalizer["Advertisement"]</option>
                            <option value="Other">@SharedLocalizer["Other"]</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="Company" class="form-label"></label>
                        <input asp-for="Company" class="form-control" />
                    </div>
                    <div class="col-md-12">
                        <label asp-for="Address" class="form-label"></label>
                        <input asp-for="Address" class="form-control" />
                    </div>
                    <div class="col-md-12">
                        <label asp-for="Notes" class="form-label"></label>
                        <textarea asp-for="Notes" class="form-control" rows="2"></textarea>
                    </div>
                </div>
            </div>
        </div>

        <div class="section-divider"><span>@SharedLocalizer["Subscription"]</span></div>
        <div class="section-title"><i class="bi bi-calendar-check"></i> @SharedLocalizer["Subscription Information"]</div>
        <div class="row g-3">
            <div class="col-md-4">
                <label asp-for="PackageId" class="form-label">@SharedLocalizer["Membership Plan"]</label>
                <select asp-for="PackageId" class="form-select" id="planSelect">
                    <option value="">@SharedLocalizer["-- Select Plan --"]</option>
                    @if (ViewBag.Plans is not null)
                    {
                        var plans = ViewBag.Plans as IEnumerable<Gym.Domain.Entities.MembershipPlan>;
                        if (plans is not null)
                        {
                            foreach (var plan in plans)
                            {
                                <option value="@plan.Id" data-price="@plan.Price.ToString("F2")" data-duration="@plan.DurationDays">@SharedLocalizer["{0} ({1} EGP)", plan.Name, plan.Price.ToString("N2")]</option>
                            }
                        }
                    }
                </select>
            </div>
            <div class="col-md-4">
                <label asp-for="OfferId" class="form-label">@SharedLocalizer["Offer"]</label>
                <select asp-for="OfferId" class="form-select" id="offerSelect">
                    <option value="">@SharedLocalizer["No offer"]</option>
                    @if (ViewBag.OfferList != null)
                    {
                        foreach (var offer in ViewBag.OfferList)
                        {
                            <option value="@offer.Id">@offer.Display</option>
                        }
                    }
                </select>
            </div>
            <div class="col-md-4">
                <label asp-for="SubscriptionPrice" class="form-label">@SharedLocalizer["Subscription Price (EGP)"]</label>
                <input asp-for="SubscriptionPrice" class="form-control" type="number" step="0.01" min="0" id="subscriptionPrice" readonly placeholder="@SharedLocalizer["0.00"]" />
            </div>
            <div class="col-md-4">
                <label asp-for="PaidAmount" class="form-label">@SharedLocalizer["Paid Amount (EGP)"]</label>
                <input asp-for="PaidAmount" class="form-control" type="number" step="0.01" min="0" id="paidAmount" placeholder="@SharedLocalizer["0.00"]" />
                <span asp-validation-for="PaidAmount" class="text-danger small"></span>
            </div>
            <div class="col-md-3">
                <label asp-for="DurationMonths" class="form-label">@SharedLocalizer["Duration (Months)"]</label>
                <input asp-for="DurationMonths" class="form-control" type="number" min="1" id="durationMonths" readonly />
            </div>
            <div class="col-md-3">
                <label asp-for="FreeMonths" class="form-label">@SharedLocalizer["Free Months"]</label>
                <input asp-for="FreeMonths" class="form-control" type="number" min="0" value="0" />
            </div>
            <div class="col-md-3">
                <label asp-for="FreezeDays" class="form-label">@SharedLocalizer["Freeze Days"]</label>
                <input asp-for="FreezeDays" class="form-control" type="number" min="0" value="0" />
            </div>
            <div class="col-md-3">
                <label asp-for="StartDate" class="form-label">@SharedLocalizer["Start Date"]</label>
                <input asp-for="StartDate" class="form-control" type="date" />
            </div>
            <div class="col-md-4">
                <label asp-for="PaymentMethod" class="form-label">@SharedLocalizer["Payment Method"]</label>
                <select asp-for="PaymentMethod" class="form-select">
                    <option value="">@SharedLocalizer["-- Select Payment --"]</option>
                    <option value="Cash">@SharedLocalizer["Cash"]</option>
                    <option value="Visa">@SharedLocalizer["Visa"]</option>
                    <option value="Instapay">@SharedLocalizer["Instapay"]</option>
                    <option value="Wallet">@SharedLocalizer["Wallet"]</option>
                    <option value="VodafoneCash">@SharedLocalizer["Vodafone Cash"]</option>
                </select>
            </div>
        </div>

        <div class="section-divider"><span>@SharedLocalizer["Medical"]</span></div>
        <div class="section-title"><i class="bi bi-heart-pulse"></i> @SharedLocalizer["Medical Information"]</div>
        <div class="row g-3">
            <div class="col-md-4">
                <div class="form-check mt-2">
                    <input asp-for="HasDisease" class="form-check-input" id="HasDisease" />
                    <label asp-for="HasDisease" class="form-check-label">@SharedLocalizer["Has Disease / Condition"]</label>
                </div>
            </div>
            <div class="col-md-4">
                <label asp-for="DiseaseType" class="form-label">@SharedLocalizer["Disease Type"]</label>
                <input asp-for="DiseaseType" class="form-control" id="DiseaseType" />
                <span asp-validation-for="DiseaseType" class="text-danger small"></span>
            </div>
            <div class="col-md-4">
                <label asp-for="Weight" class="form-label">@SharedLocalizer["Weight (kg)"]</label>
                <div class="input-group">
                    <input asp-for="Weight" class="form-control" type="number" step="0.1" min="0" />
                    <span class="input-group-text">@SharedLocalizer["kg"]</span>
                </div>
            </div>
        </div>

        <div class="section-divider"><span>@SharedLocalizer["Fingerprint"]</span></div>
        <div class="section-title"><i class="bi bi-fingerprint"></i> @SharedLocalizer["Fingerprint"]</div>
        <div class="row g-3">
            <div class="col-md-6">
                <label asp-for="FingerprintDeviceId" class="form-label">@SharedLocalizer["Device ID"]</label>
                <input asp-for="FingerprintDeviceId" class="form-control" type="text" placeholder="@SharedLocalizer["GUID (optional)"]" />
            </div>
        </div>

        <div class="section-divider"><span>@SharedLocalizer["Signatures"]</span></div>
        <div class="section-title"><i class="bi bi-pen"></i> @SharedLocalizer["Signatures"]</div>
        <div class="row g-3">
            <div class="col-md-6">
                <label asp-for="MemberSignature" class="form-label">@SharedLocalizer["Member Signature"]</label>
                <textarea asp-for="MemberSignature" class="form-control" rows="3"></textarea>
            </div>
            <div class="col-md-6">
                <label asp-for="AdminSignature" class="form-label">@SharedLocalizer["Admin Signature"]</label>
                <textarea asp-for="AdminSignature" class="form-control" rows="3"></textarea>
            </div>
        </div>

        <div class="receipt-footer">
            <a asp-action="Index" class="btn btn-secondary"><i class="bi bi-x-lg"></i> @SharedLocalizer["Cancel"]</a>
            <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg"></i> @SharedLocalizer["Update Member"]</button>
        </div>
    </form>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const hasDisease = document.getElementById('HasDisease');
            const diseaseType = document.getElementById('DiseaseType');

            hasDisease.addEventListener('change', function() {
                diseaseType.disabled = !this.checked;
                if (!this.checked) diseaseType.value = '';
            });

            if (!hasDisease.checked) diseaseType.disabled = true;

            const dropzone = document.getElementById('imageDropzone');
            const input = document.getElementById('imageInput');
            const placeholder = document.getElementById('dropzonePlaceholder');
            const preview = document.getElementById('dropzonePreview');
            const img = document.getElementById('imagePreview');
            const removeBtn = document.getElementById('removeImage');

            function showPreview(file) {
                if (!file) return;
                const reader = new FileReader();
                reader.onload = function(e) {
                    img.src = e.target.result;
                    placeholder.style.display = 'none';
                    preview.style.display = 'block';
                    dropzone.classList.add('has-image');
                };
                reader.readAsDataURL(file);
            }

            dropzone.addEventListener('click', function() { input.click(); });
            dropzone.addEventListener('dragover', function(e) { e.preventDefault(); dropzone.classList.add('dragover'); });
            dropzone.addEventListener('dragleave', function() { dropzone.classList.remove('dragover'); });
            dropzone.addEventListener('drop', function(e) {
                e.preventDefault();
                dropzone.classList.remove('dragover');
                if (e.dataTransfer.files.length) {
                    input.files = e.dataTransfer.files;
                    showPreview(e.dataTransfer.files[0]);
                }
            });
            input.addEventListener('change', function() {
                if (this.files.length) showPreview(this.files[0]);
            });
            removeBtn.addEventListener('click', function(e) {
                e.stopPropagation();
                input.value = '';
                placeholder.style.display = '';
                preview.style.display = 'none';
                dropzone.classList.remove('has-image');
            });

            const planSelect = document.getElementById('planSelect');
            const offerSelect = document.getElementById('offerSelect');
            const priceInput = document.getElementById('subscriptionPrice');
            const paidInput = document.getElementById('paidAmount');
            const durationInput = document.getElementById('durationMonths');

            @{
                var plansJson = System.Text.Json.JsonSerializer.Serialize(
                    ((IEnumerable<Gym.Domain.Entities.MembershipPlan>)ViewBag.Plans)
                        .Select(p => new {
                            id = p.Id.ToString(),
                            price = p.Price,
                            durationDays = p.DurationDays
                        }).ToList()
                );
                var offersJson = System.Text.Json.JsonSerializer.Serialize(
                    ((IEnumerable<dynamic>)ViewBag.OfferList)
                        .Select(o => new {
                            id = ((object)o.Id).ToString(),
                            linkedPackageId = o.LinkedPackageId?.ToString(),
                            offerType = o.OfferType.ToString(),
                            offerPrice = o.OfferPrice != null ? (decimal?)o.OfferPrice : null
                        }).ToList()
                );
            }
            var plans = @Html.Raw(plansJson);
            var offers = @Html.Raw(offersJson);

            function getPlanData(planId) {
                return plans.find(function(p) { return p.id === planId; });
            }

            function getOfferData(offerId) {
                return offers.find(function(o) { return o.id === offerId; });
            }

            function getPriceFromPlanOrOffer() {
                if (offerSelect && offerSelect.value) {
                    var od = getOfferData(offerSelect.value);
                    var planId = planSelect ? planSelect.value : '';
                    var pd = planId ? getPlanData(planId) : null;
                    if (!pd) return 0;
                    if (od) {
                        if (od.offerType === 'FixedPrice') return od.offerPrice != null ? od.offerPrice : pd.price;
                        if (od.offerType === 'FreeRegistration') return 0;
                        return pd.price;
                    }
                    return pd.price;
                }
                if (planSelect && planSelect.value) {
                    var pd = getPlanData(planSelect.value);
                    return pd ? pd.price : 0;
                }
                return 0;
            }

            function getDurationFromPlanOrOffer() {
                var planId = null;
                if (offerSelect && offerSelect.value) {
                    var od = getOfferData(offerSelect.value);
                    if (od && od.linkedPackageId) planId = od.linkedPackageId;
                }
                if (!planId && planSelect && planSelect.value) planId = planSelect.value;
                if (planId) {
                    var pd = getPlanData(planId);
                    if (pd) {
                        var months = Math.round(pd.durationDays / 30);
                        return months || 1;
                    }
                }
                return 1;
            }

            function updatePriceAndDuration() {
                var price = getPriceFromPlanOrOffer();
                priceInput.value = price.toFixed(2);
                paidInput.value = price.toFixed(2);
                durationInput.value = getDurationFromPlanOrOffer();
            }

            function enablePlan(val) {
                if (planSelect) {
                    planSelect.disabled = !val;
                    if (!val) planSelect.value = '';
                }
            }

            function enableOffer(val) {
                if (offerSelect) {
                    offerSelect.disabled = !val;
                    if (!val) offerSelect.value = '';
                }
            }

            function onPlanChange() {
                if (planSelect && planSelect.value) {
                    enableOffer(false);
                } else {
                    enableOffer(true);
                }
                updatePriceAndDuration();
            }

            function onOfferChange() {
                if (offerSelect && offerSelect.value) {
                    enablePlan(false);
                    var od = getOfferData(offerSelect.value);
                    if (od && od.linkedPackageId && planSelect) {
                        planSelect.value = od.linkedPackageId;
                    }
                } else {
                    enablePlan(true);
                }
                updatePriceAndDuration();
            }

            if (planSelect) {
                planSelect.addEventListener('change', function() {
                    onPlanChange();
                });
            }

            if (offerSelect) {
                offerSelect.addEventListener('change', function() {
                    onOfferChange();
                });
            }

            updatePriceAndDuration();
        });
    </script>
}
